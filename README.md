# metalanguage

Open ended RSI

## Utilities

- `utils/openrouter.py`: helpers for OpenRouter Responses API calls.
- `utils/hf_datasets.py`: `download_hf_dataset_to_file(...)` writes a Hugging Face dataset split to JSONL.
