# metalanguage
Open ended RSI

## Utilities

- `chat_util.py`: Includes `chat_text(...)`, a helper that sends text chat messages to a chat-completions-compatible endpoint using a custom URL and API key.
