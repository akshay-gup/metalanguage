[project]
name = "metalanguage"
version = "0.1.0"
description = "Open-ended RSI rollout loop."
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "datasets>=2.20",
    "filelock>=3.13",
    "requests>=2.31",
]

[tool.uv]
package = false
